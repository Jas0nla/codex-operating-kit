# Board Registry Entry

- `board_name`: `evaluation-observability`
- `owner_agent`: `评估与观测官`
- `required_context`: `board state`, `handoff outcomes`, `agent return quality`, `rework traces`
- `forbidden_context`: `raw full-thread memory`, `creative draft content unless needed for defect diagnosis`
- `success_signal`: `visible quality signal on where the system is holding or degrading`
- `handoff_fields`: `board stage`, `owner explicit`, `last handoff status`, `latest rework cause`, `source surfaces used`
