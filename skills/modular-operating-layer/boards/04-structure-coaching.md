# Board Registry Entry

- `board_name`: `structure-coaching`
- `owner_agent`: `MAIC 结构教练`
- `required_context`: `topic`, `current outline`, `core claims`, `target comprehension level`
- `forbidden_context`: `full publishing workflow`, `sensitive-word raw logs`, `distribution metrics noise`
- `success_signal`: `reordered explanation path with clarified reasoning gaps`
- `handoff_fields`: `input outline`, `recommended structure`, `open understanding gaps`, `examples needed`, `source surfaces used`
