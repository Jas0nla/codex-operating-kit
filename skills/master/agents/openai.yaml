interface:
  display_name: "Master"
  short_description: "Route private workflows and apply private delegation refinement"
  default_prompt: "Use $master to route this request into the matching private workflow, apply private delegation refinement after the global baseline, and follow the most specific local skill and memory rules first."

policy:
  allow_implicit_invocation: true
